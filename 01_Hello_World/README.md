# 01 — Hello World (Docker Basics)

Objectif : construire et exécuter une image Docker qui lance un script Python.

Commandes :

- `docker build -t 01_hello_world .`
- `docker run --rm 01_hello_world`
