# 05 — Microservices (Frontend ↔ Backend)

Objectif : orchestrer 2 services (frontend + backend) sur un réseau Docker interne.

Installation :

```bash
cp .env.example .env
docker compose up --build
```

Test : ouvre `http://localhost:8000`.
